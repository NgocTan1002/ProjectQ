# =============================================================================
# Production requirements — kế thừa base
# =============================================================================
-r base.txt

gunicorn>=22.0,<23              # WSGI server
whitenoise[brotli]>=6.7,<7     # Serve static files without Nginx for simple deploys
django-storages[s3]>=1.14,<2   # S3 media storage
sentry-sdk[django]>=2.0,<3     # Error monitoring
